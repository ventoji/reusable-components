<!--- Password.stories.mdx -->
import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks';
import PasswordInput from '../components/PasswordInput/index';
import { action } from '@storybook/addon-actions'; 

<Meta title="React Library/Forms Elements/Password" component={PasswordInput} />


# Password field


<!--- This is your Story template function, shown here in React -->

export const Template = (args) => {
     class PasswordExtension extends React.Component {
        constructor(props) {
            super(props);
            this.state = {
                password: '',
            };
     }
        onChangeHandle(event){
            this.setState({ password: event.target.value }, ()=> console.log(this.state.password));
        }
        
        getQuality() {
            const length = this.state.password.length;
            return length > 10 ? 100 : length * 10;
        }
        render(){
            // console.log(args);
            return <PasswordInput 
             {...args}
             value={this.state.password} 
             onChange={this.onChangeHandle.bind(this)}  
            quality={this.getQuality()}
            />;
        }
     };
    return <PasswordExtension />
    };

<Canvas>
 <Story name="Password Field" args={{ 
       htmlId:'password-input-example-all-features',
        showVisibilityToggle: true,
        name:'Password',
        minLength: 8,
        placeholder:'Enter password'
    }}>
    {Template.bind({})}
   </Story>
</Canvas>

# Parameters 

<ArgsTable of={PasswordInput} />
